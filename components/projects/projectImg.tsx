import React from 'react';
import styles from "./projects.module.css"

const ProjectImg = () => {
    return (
        <div className={styles.projectImgStyle}>
        </div>
    );
};

export default ProjectImg;
